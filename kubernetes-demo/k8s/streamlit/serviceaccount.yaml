apiVersion: v1
kind: ServiceAccount
metadata:
  name: streamlit
  namespace: data-platform
# Streamlit never talks to the Kubernetes API, so it must not receive an
# API token at all.
automountServiceAccountToken: false
