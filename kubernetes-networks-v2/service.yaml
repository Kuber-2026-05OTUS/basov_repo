apiVersion: v1
kind: Service
metadata:
  name: basov-service
  namespace: homework
spec:
  type: ClusterIP
  selector:
    app: basov
  ports:
    - name: tcp
      port: 8000
      targetPort: 8000
      protocol: TCP